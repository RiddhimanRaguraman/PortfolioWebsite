{
  "name": "riddhiman-portfolio",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --root .. --port=3000 --host=0.0.0.0",
    "build": "vite build --root ..",
    "preview": "vite preview --root ..",
    "lint": "echo 'No linting needed for static HTML'"
  },
  "dependencies": {
    "vite": "^6.2.0"
  }
}
